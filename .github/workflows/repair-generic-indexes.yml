name: Repair generic INDEX files
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  repair:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled. Generic repair is no longer allowed to rewrite completed tools automatically."
