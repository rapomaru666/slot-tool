name: Individualize slot tools
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  individualize:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled. Tools are updated individually under RAPOMAN master control."
