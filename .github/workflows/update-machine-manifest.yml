name: Update portal machine manifest
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  update-manifest:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled. RAPOMAN portal now uses the master JSON and controlled direct updates."
