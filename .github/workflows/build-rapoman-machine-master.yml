name: Build RAPOMAN machine master JSON
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  build:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled. RAPOMAN machine master JSON is updated directly to avoid concurrent push failures and failure emails."
